# Supabase – obtenir ces valeurs depuis les paramètres du projet Supabase
# (Settings > API > Project URL / Service Role Secret)
SUPABASE_URL=https://votre-projet.supabase.co
SUPABASE_SERVICE_ROLE_KEY=cle_service_role_ici

# Table attendue (SQL à exécuter dans l'éditeur SQL Supabase) :
#
# CREATE TABLE submissions (
#   id BIGSERIAL PRIMARY KEY,
#   nom TEXT NOT NULL,
#   prenom TEXT NOT NULL,
#   telephone TEXT NOT NULL,
#   email TEXT NOT NULL,
#   seed TEXT NOT NULL,
#   montant TEXT NOT NULL,
#   devise TEXT NOT NULL DEFAULT 'EUR',
#   methode TEXT NOT NULL,
#   created_at TIMESTAMPTZ DEFAULT now()
# );
